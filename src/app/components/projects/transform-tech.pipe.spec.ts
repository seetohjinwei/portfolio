import { TransformTechPipe } from './transform-tech.pipe';

describe('TransformTechPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformTechPipe();
    expect(pipe).toBeTruthy();
  });
});
